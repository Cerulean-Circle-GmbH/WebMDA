# WebMDA Project Prompts

This file contains the **literal quoted prompts** used to create the WebMDA project, as requested by the user.

---

## 📌 Initial Request

> **User Message (id: 34005f15-60f5-4979-8e33-478721d56965)**
> **Request**:
> read the description and give me your best first shot

---

## 📌 First Shot Feedback

> **User Message (id: 63db77d8-9525-4084-ae74-0c67e07d7177)**
> **Request**:
> its a very good first shot. yes, use SVG instead of canvas. keep the model in sync with a typescript code and a puml code in the same time. make sure everything is radical OOP itself. prvent any non OOP Types in typescript. all calsses must have parameterless consctructors. all classes have an init() mehtod, that inititaliyes their model = {...}. the model/state in all classes is a naked json containing the attributes. classes expose these via getters and setters. each class always has name,description, badge, icon, uuid as UUIDv4 as attributes in the model enforce this by an Item interface.

---

## 📌 Final Request

> **User Message (id: 3cd98f5e-6f16-49cd-8741-7394cd1d056e)**
> **Request**:
> yes create it fully in git. but here cd /workspace/WebMDA
> nobody shall know anything about the project to be able to run it
> so it needs to be just npm start and the server starts...compliled berfore or not. jus ONE command.

---

## 📌 README Request

> **User Message (id: 7a46238b-ecea-48e3-9970-fc91892ddd85)**
> **Request**:
> write it in the readme. and make a nice project description after the HOW to run

---

## 📌 Final Request

> **User Message (id: 0c3b47bc-475b-4b40-a1c9-11c07e3c308a)**
> **Request**:
> commit and push after each step. also create a md file with my literal quted prompts to create the project.

---

## 📝 Notes

- All prompts are **quoted verbatim** as provided by the user.
- The project was created in `/workspace/WebMDA` and pushed to `Cerulean-Circle-GmbH/WebMDA`.
- Each major step was committed and pushed to GitHub.
